This directory is expected to contain your base resume, user-maintained and version-controlled here:

- `main.tex` — your resume, LaTeX source
- `resume.cls` — its class file

`/job-hunt` reads `main.tex` for relevance scoring and as the source the resume-tailor subagent edits per job. Neither file is generated by this project — add your own before running `/job-hunt`.
