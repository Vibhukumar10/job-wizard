.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/

# Runtime output — regenerated per run, contains tailored resumes (PII)
runs/

# Dedup log — mutable runtime state, not source
state/seen-jobs.json

# Notion Job Tracker database_id, created on first /job-hunt run — mutable runtime state, not source
state/notion-tracker.json
